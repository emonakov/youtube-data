import { colors } from './colors';

export const theme = {
  paddingContent: '20px',
  paddingSm: '5px',
  paddingMd: '15px',
  backgroundColor: colors.backgroundColor,
  loadingColor: colors.loadingColor,
};
