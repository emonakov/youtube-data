import React from 'react'
import styled from 'styled-components'

const Title = styled.h1`
  text-align: center;
`

const NotFound: React.FC = () => <Title>Page not found</Title>

export default NotFound
